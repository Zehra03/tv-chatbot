import { PagePlaceholder } from '@/components/PagePlaceholder'

/** /chat — Chatbot ana ekranı. Gerçek implementasyon Epic 5'te. */
export function ChatPage() {
  return <PagePlaceholder title="Chatbot" route="/chat" />
}
