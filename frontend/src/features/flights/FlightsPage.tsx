import { PagePlaceholder } from '@/components/PagePlaceholder'

/** /flights — Uçuş sonuçları. Gerçek implementasyon Epic 6'da. */
export function FlightsPage() {
  return <PagePlaceholder title="Uçuş sonuçları" route="/flights" />
}
