import { PagePlaceholder } from '@/components/PagePlaceholder'

/** /reservations — Rezervasyon listesi. Gerçek implementasyon Epic 7'de. */
export function ReservationsPage() {
  return <PagePlaceholder title="Rezervasyonlarım" route="/reservations" />
}
