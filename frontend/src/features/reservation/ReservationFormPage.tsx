import { PagePlaceholder } from '@/components/PagePlaceholder'

/** /reservation/new — Kontrollü rezervasyon formu. Gerçek implementasyon Epic 7'de. */
export function ReservationFormPage() {
  return <PagePlaceholder title="Rezervasyon formu" route="/reservation/new" />
}
